api_base_url: "http://localhost:8000"
auth:
  enabled: false
broker:
  url: "redis://localhost:6379/0"
db:
  url: "postgresql://postgres:postgres@localhost:5432/especialista_contabilidade"
  vectors: "postgresql://postgres:postgres@localhost:5432/especialista_contabilidade_vectors"
  checkpoints: "postgresql://postgres:postgres@localhost:5432/especialista_contabilidade_checkpoints"
vault:
  url: "http://localhost:8200"
  token: "dev-only-token"
