from agent_lab import Container as BaseContainer


class Container(BaseContainer):
    """Dependency-injection container for Especialista Contabilidade.

    Add your own providers here (services, repositories, clients) and name
    them in an agent's or registrar's ``extra_deps`` to have them injected.
    """
